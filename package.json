{
  "name": "ec.cookie-mock",
  "version": "1.0.1",
  "description": "Mock implementation for document.cookie.",
  "main": "./src/CookieMock.js",
  "author": "Simon Scherzinger <scherzinger@entrecode.de>",
  "license": "ISC",
  "repository": "entrecode/ec.cookie-mock",
  "devDependencies": {
    "c8": "^11.0.0",
    "mocha": "^11.7.5"
  },
  "scripts": {
    "test": "mocha",
    "test:coverage": "c8 --reporter=text --reporter=lcov npm test"
  },
  "c8": {
    "exclude": [
      "test/**"
    ]
  }
}
